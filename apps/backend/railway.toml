[build]
builder = "nixpacks"
buildCommand = "pip install -e ../../packages/nse_engine && pip install -r requirements.txt"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
